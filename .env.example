# Use relative API path in frontend and route through Vite dev proxy
VITE_API_BASE_URL=
# Local backend target (commented)
# VITE_DEV_PROXY_TARGET=http://localhost:8000
# Public tunnel backend target (active)
VITE_DEV_PROXY_TARGET=https://unrescinded-recreatively-joselyn.ngrok-free.dev
VITE_USE_MOCK_BACKEND=false
